# git pull & sass build
```
git clone https://github.com/blackraccoon000/damp.git
npm install
npm run sass
```

# docker-compose up
```
docker-compose up -d --build
```

# docker-compose down
```
docker-compose up -d --build
```